{% extends 'base.html.twig' %}

{% block title %}Notice{% endblock %}

{% block body %}
    <h1>Notice</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ notice.id }}</td>
            </tr>
            <tr>
                <th>TitleNotice</th>
                <td>{{ notice.titleNotice }}</td>
            </tr>
            <tr>
                <th>PriceNotice</th>
                <td>{{ notice.priceNotice }}</td>
            </tr>
            <tr>
                <th>DescriptionNotice</th>
                <td>{{ notice.descriptionNotice }}</td>
            </tr>
            <tr>
                <th>LocationNotice</th>
                <td>{{ notice.locationNotice }}</td>
            </tr>
            <tr>
                <th>PhotoOneNotice</th>
                <td>{{ notice.photoOneNotice }}</td>
            </tr>
            <tr>
                <th>PhotoTwoNotice</th>
                <td>{{ notice.photoTwoNotice }}</td>
            </tr>
            <tr>
                <th>PhotoThreeNotice</th>
                <td>{{ notice.photoThreeNotice }}</td>
            </tr>
            <tr>
                <th>DateNotice</th>
                <td>{{ notice.dateNotice ? notice.dateNotice|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>CategoryProfessionnalNotice</th>
                <td>{{ notice.categoryProfessionnalNotice ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('notice_index') }}">back to list</a>

    <a href="{{ path('notice_edit', {'id': notice.id}) }}">edit</a>

    {{ include('notice/_delete_form.html.twig') }}
{% endblock %}
